export class Player {
    nickname: string;
    car: string;
    key: string;

    constructor(nickname: string, car: string, key: string) {
        this.nickname = nickname;
        this.car = car;
        this.key = key;
    }
}
